import React, { useState, useEffect } from 'react';

const Timer = () => {
  const [duration, setDuration] = useState(0);

  useEffect(() => {});

  return (
    <div>
      <p>{}</p>
    </div>
  );
};
